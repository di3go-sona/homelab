apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pastefy
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/di3go-sona/homelab.git
    targetRevision: main
    path: src/argo/apps/pastefy/resources
  destination:
    server: https://kubernetes.default.svc
    namespace: pastefy
  syncPolicy:
    # automated:
    #   prune: true
    #   selfHeal: true
    syncOptions:
      - CreateNamespace=true
